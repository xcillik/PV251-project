import { describe, expect, it } from "vitest";

describe("Sample Test B", () => {
    it("should pass", () => {
        expect(1 + 1).toBe(2);
    });

    it("should pass", () => {
        expect(2 + 2).toBe(4);
    });

    it("should pass", () => {
        expect(1 + 5).toBe(6);
    });

    it("should pass", () => {
        expect(5 + 5).toBe(10);
    });
});
